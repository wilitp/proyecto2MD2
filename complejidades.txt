# complejidad getColorInfo

O(n + r + n) = O(n)

# complejidad orden jedi

tiempo:

O(n + n + r + rlog(r) + n + r) = O(n + rlog(r))
colorInfo + computarF + initOrdenColores + ordenarColores + aplanarV + colorInfoDestroy

# complejidad orden impar par

O(n + n + r) = O(n)
colorInfo + aplanarV + colorInfoDestroy
